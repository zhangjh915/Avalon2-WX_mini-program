# 《圆桌秘闻》多人版部署与体验说明

## 需要的设备

- Mac：安装微信开发者工具，用于编译、部署云函数和上传体验版。
- 手机：每位玩家用自己的微信扫码加入并完成私密操作。

项目已经绑定：

- AppID：`wx657aadc4dc40357f`
- 云环境：`cloudbase-d6gh6eo4ce2c13e2b`

## 首次部署

### 1. 导入项目

在微信开发者工具选择“小程序”，导入目录：

```text
/Users/zyb/Documents/ChatGPT/阿瓦隆2
```

### 2. 创建数据库集合

在云开发控制台的“数据库”中新建：

```text
avalon_rooms
avalon_game_secrets
```

权限建议：

- `avalon_rooms`：所有用户仅可读，客户端不可写。
- `avalon_game_secrets`：所有用户不可读、不可写。

所有写入和私密读取都通过 `avalonGame` 云函数完成。不要把 `avalon_game_secrets` 设为所有用户可读。

### 3. 部署云函数

在微信开发者工具左侧展开 `cloudfunctions`：

1. 右键 `avalonGame`。
2. 选择“上传并部署：云端安装依赖”。
3. 等待部署完成。

该云函数使用数据库事务，并调用 `wxacode.getUnlimited` 生成房间小程序码。若控制台询问 OpenAPI 权限，请允许“小程序码”。

### 4. 清理旧测试房间

旧版房间文档与当前私密架构不兼容。部署后直接创建新房间，不要继续旧房间。

## 开发工具单人测试

1. 点击“编译”。
2. 首页填写昵称，选择人数、桌型和角色。
3. 创建房间，选择一个座位。
4. 点击“补满测试玩家”。
5. 点击“开始游戏”。

测试玩家会自动完成身份确认、护身符和终局等必须动作。任务投票仍由房主在页面下方替测试玩家选择成功或失败，便于主动测试比分和翻盘。

小程序码在开发版环境可能生成失败，这是微信 OpenAPI 对版本的限制；本地测试可继续使用6位房间码。

## 手机多人体验

### 1. 上传体验版

1. 在开发者工具点击“上传”。
2. 到微信公众平台将该版本设为体验版。
3. 把同事加入“体验成员”。

### 2. 加入房间

1. 房主打开体验版，创建房间并入座。
2. 候场页展示房间小程序码和6位房间码。
3. 同事微信扫码直达房间，或打开小程序输入房间码。
4. 第一次扫码的玩家先填写昵称，再点击与现实位置对应的座位。

### 3. 实际流程

1. 全员密封忠诚牌并同步查看身份、额外信息和攻略。
2. 房主播放首夜主持词，指定角色在现实中睁眼相认。
3. 当前队长在自己的手机选择同行骑士和魔法指示物。
4. 上车玩家在自己的手机私密提交任务牌。
5. 当前队长在自己的手机交接皇冠和护身符。
6. 护身符持有者私密选择查验对象，被查者在自己的手机回应。
7. 任一阵营达到三次任务胜利后，按配置进入猎杀或最终指认。
8. 结束后全员进入身份公开和任务档案页。

## 常见问题

### 修改代码后手机没有变化

云函数改动必须重新“上传并部署”，只点击编译不会更新云端逻辑。

### 创建房间提示集合不存在

确认两个集合都已创建，尤其是：

```text
avalon_game_secrets
```

### 房间小程序码不显示

先确认云函数已重新部署且已获小程序码 OpenAPI 权限。开发环境受限时使用房间码，上传体验版后再验证扫码。

### 操作后短暂显示“同步中”

选择座位、组队和选择目标会先本地响应，再由云函数确认。任务票和最终指认以服务器事务为准，用于防止多人同时提交时互相覆盖。
