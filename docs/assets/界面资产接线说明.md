# 界面资产接线说明

> 给代码会话的交接文档。资产已导出并入库，这份文档说明**每个文件接到哪里、怎么接**。
>
> 资产生产过程见 [生图工作手册](生图工作手册.md)，选择理由见
> `docs/assets/prompts/_选择/界面.json`。

---

## 0. 资产清单与取图方式

### 走云存储，不进主包

本地文件在 `miniprogram/assets/ui/`，但已通过 `project.config.json` 的
`packOptions.ignore` **排除出包体**——和角色立绘、身份牌卡背同样处理。
本地那份只是上传中转。

原因：这批资产 1.11 MB，而主包上限 2 MB、代码本身才 274 KB。
全放进包里会占掉 72%，只剩 550 KB 给后续所有功能。排除之后主包是 **370 KB（18%）**。

**所以落地前必须先把 `miniprogram/assets/ui/` 整个目录上传到云存储**，
路径保持 `assets/ui/...` 不变（和 `assets/roles/`、`assets/cards/identity/` 同级）。

上传用 `scripts/upload-cloud-assets.js`（参数是相对 `miniprogram/` 的路径，
同时也就是云存储上的 cloudPath）：

```
node scripts/upload-cloud-assets.js assets/ui/floor-green.jpg assets/ui/floor-wine.jpg
```

两个坑：devtools 的 `cli cloud` 只有 `env` 和 `functions`，**没有 storage**，
所以上传只能在小程序上下文里调 `wx.cloud.uploadFile`，脚本走 automator 注入；
而 `uploadFile` 的 `filePath` **不认宿主机绝对路径**（报 `createUploadTask:fail
file not found`），必须先把文件 base64 搬进 `wx.env.USER_DATA_PATH` 再传。

`assets.js` 加一个取图函数，和现有的 `roleArt` / `identityBack` 同一个写法：

```js
// 界面图标与场景。和角色立绘一样走云存储，路径即 fileID 后缀。
function uiAsset(name) {
  return CLOUD_PREFIX + "assets/ui/" + name
}
```

下表「接到哪」里的路径都改用 `assets.uiAsset("xxx.png")` 取，
**不要写 `/assets/ui/xxx.png` 这种包内绝对路径**——排除出包后取不到。

wxss 里的 `background: url(...)` 同理不能用包内路径。三个办法：
（a）改成 `<image>` 节点，src 走 `uiAsset()`；
（b）在 js 里算好 fileID，用 `style="background-image:url({{...}})"` 内联；
（c）体积特别小、又是首屏关键路径的，单独留在包内。
桌子和地面建议走 (a) 或 (b)，见 §3、§4。

| 文件（云端 `assets/ui/` 下） | 尺寸 | 接到哪 |
|---|---|---|
| `crown.png` | 96px | `.crown-icon` 👑（队长/下任队长） |
| `amulet.png` | 224px | `.amulet-icon` 🧿（查验权），**褪色态复用同一张走 CSS 滤镜** |
| `magic-fire.png` | 224px | `.fire-icon` 🔥（魔法指示物） |
| `table-emblem.png` | 192px | `.table-emblem` 桌心「杯」 |
| `crest-good.png` | 240px | 正义队徽：`.mission-success` + `.verdict-mark`（正义胜） |
| `crest-evil.png` | 240px | 邪恶队徽：`.mission-fail` + `.verdict-mark`（邪恶胜） |
| `mission-pending.png` | 128px | `.mission-orb` 待打态 |
| `mission-current.png` | 128px | `.mission-orb` 当前轮 |
| `home-emblem.png` | 160px | `.home-emblem` 首页「杯」 |
| `seal-base.png` | 208px | 9 个状态印的**共用底纹**，汉字继续叠在上面 |
| `table-round.jpg` | 512² | `.round-table` |
| `table-long/` | 9 块 | `.long-table`，见 §3 |
| `floor-green.jpg`<br>`floor-indigo.jpg`<br>`floor-wine.jpg`<br>`floor-umber.jpg` | 各 1080² | `.compact-table-stage` 台面，**四色玩家可选**，见 §4.1 |
| `home-bg.jpg` | 750×1624 | 首页整屏底 |

---

## 1. 直接替换 emoji / 汉字的（最简单的一批）

| 位置 | 现在 | 换成 |
|---|---|---|
| `play.wxml:23,135,221,241` | `<text class="symbol-icon crown-icon">👑</text>` | `<image src="{{ui.crown}}" mode="aspectFit" />`（js 里 `ui.crown = assets.uiAsset("crown.png")`） |
| `play.wxml:139,152,159,221` | `🧿` | `amulet.png` |
| `play.wxml:99,221,243` | `🔥` | `magic-fire.png` |
| `play.wxml:199,218` + `room.wxml:20` | `.table-emblem` 里的「杯」 | `table-emblem.png` |
| `index.wxml:3` | `.home-emblem` 里的「杯」 | `home-emblem.png` |

`.symbol-icon` 的 `font-family` / `color` 规则（`play.wxss:1489-1492`）换成图片后失效，可以删。

**褪色护身符不换新图。** `.faded-badge` 保留现有 `filter: grayscale(1); opacity: .62`，
作用在同一张 `amulet.png` 上。理由：它只在 `play.wxml:227` 一处渲染、字号 `23rpx = 34 物理像素`，
裂纹在这个尺寸画不出来，而 CSS 灰度天然保证与完好版同角度同构图。

---

## 2. 任务指示器 `.mission-orb`（play.wxml:6）

四态**全部换成图片**，原来的圆形边框和背景色要一起去掉——四张图各自带完整的圆形外观。

```
待打    mission-pending.png     暗石板封印
当前轮  mission-current.png     点亮的金色封印
成功    crest-good.png          正义队徽（鹰翼环抱圣杯）
失败    crest-evil.png          邪恶队徽（蛇身盘环嵌宝石）
```

要删的 CSS（`play.wxss:98-145`）：`.mission-orb` 的 `border` / `border-radius` /
`background` / `color`，以及 `.mission-success` / `.mission-fail` / `.mission-current`
的底色和边框色。里面的 `.mission-symbol`（`{{item.label}}`）也不再需要。

**两条容易做错的：**

1. **队徽不要裁成圆、也不要外加圆框。** 正义那枚是桃形、邪恶那枚是圆环，
   这是设计本身，按原始轮廓直接摆。两枚长宽比是 0.89 / 0.95，排成一行不会高低不齐。
2. **底色不能再用绿/红。** 阵营色现在由队徽自己承载，绿徽压绿底会糊成一团。

> `mission-current.png` 是**接近旋转对称**的（旋转 15° 平均通道差 13.6），
> 可以用 CSS `animation: rotate` 表达「进行中」。`mission-pending.png` 旋转差只有 4.8，
> 转起来看不出来，不要给它加旋转。

---

## 3. 桌子

### 3.1 圆桌 `.round-table`（play.wxss:1271）

`table-round.jpg` **是带黑色四角的 JPEG**（存 JPEG 是为了从 472KB 降到 51KB）。

**必须保留 `border-radius: 50%`**，否则四角的黑块会露出来。

```css
.round-table {
  border: 0;                       /* 桌沿由图提供，删掉原来的 7rpx 描边 */
  border-radius: 50%;              /* 必须留，用来裁掉 JPEG 的黑四角 */
  overflow: hidden;
  /* 投影必须保留！桌面和地面都是正俯视平光，没有投影就完全没有深度线索，
     桌子会像一张贴纸浮在地毯上。实机验证过这一条。 */
  box-shadow: 0 14rpx 25rpx rgba(20, 14, 10, 0.45);
  /* 背景走云存储取不到包内路径，改成内联 background-image，或塞一个 <image> 铺满 */
}
```

> **只删 `border`，不要删 `box-shadow`。** 原来的投影是
> `0 14rpx 25rpx rgba(59,44,31,0.22)`，地面换成深色绒毯后要加深到上面那个值才看得出来。
> 长桌同理。

wxml 里内联（`roundTableBg` 在 js 里 = `assets.uiAsset("table-round.jpg")`）：

```html
<view class="round-table" style="background-image:url({{roundTableBg}})"></view>
```

### 3.2 长桌 `.long-table` —— 九宫格

`.long-table` 的长宽比**不是几个预设，是 30 种连续值**（`tableLayout.js:79-82`：
宽只看上下边人数、高只看左右边人数，两个参数独立，是笛卡尔积；比值 1.00–2.43，
再乘 stage 自身 1.19 的宽高比，真实像素比 1.19–2.89）。整图拉伸会把桌沿和铆钉抻变形，
所以切了九宫格：**四角固定像素、四边只沿单轴平铺、中心 cover**。

**两个数字必须严格配对，写错一个角上就会缺一块：**

| 参数 | 值 | 依据 |
|---|---|---|
| 切角 `background-size` | **`56rpx`**（= 84 物理像素 @3x） | 必须 ≥ 圆弧半径，否则角块自己把弧切掉 |
| `border-radius` | **`51rpx`**（= 76 物理像素 @3x） | 必须**等于**角块里那道圆弧的半径 |

原理：CSS 的 `repeat-x` / `repeat-y` 会从 position 起向**两个方向**无限平铺，
也就是会铺到角块底下。角块只在自己那道弧以内不透明，弧以外是透明的。
所以 `border-radius` 必须正好裁在弧上——

- **`border-radius` 小于弧半径**：弧和裁剪线之间留下一圈月牙，CSS 保留但角块没画，
  底下的边条和中心木纹露出来，看着像角上缺了一块。这正是第一版的 bug
  （角块 72px、弧 78px、`border-radius` 32rpx=48px，差了 30px）。
- **大于弧半径**：把角饰本身裁掉一截。

改桌子图之后这两个数会变，**必须重新量**，别照抄：

```bash
# 切片（inset 要大于源图的圆角半径，这张实测约 238 源像素，所以给 260）
python3 scripts/slice9.py <抠好的桌子.png> --out <目录> --inset 260 --render-inset 84
```
量出来的弧半径会打印在导出日志里，`scripts/export-assets.py` 的 `SLICE_OUT` 同步改。

九个 url 都在云端，wxss 里写不了，得在 js 里拼好整段 `background` 再内联：

```js
// assets.js
const LONG_TABLE_SLICES = [
  ["corner_tl.png", "left top",     "56rpx 56rpx", "no-repeat"],
  ["corner_tr.png", "right top",    "56rpx 56rpx", "no-repeat"],
  ["corner_bl.png", "left bottom",  "56rpx 56rpx", "no-repeat"],
  ["corner_br.png", "right bottom", "56rpx 56rpx", "no-repeat"],
  ["edge_top.jpg",    "56rpx top",    "auto 56rpx", "repeat-x"],
  ["edge_bottom.jpg", "56rpx bottom", "auto 56rpx", "repeat-x"],
  ["edge_left.jpg",   "left 56rpx",   "56rpx auto", "repeat-y"],
  ["edge_right.jpg",  "right 56rpx",  "56rpx auto", "repeat-y"],
  ["center.jpg",      "center",       "cover",      "no-repeat"],
]

// 顺序即层序：先列的画在上面，所以角在最上、边其次、中心垫底。不要重排。
function longTableBackground() {
  return "background:" + LONG_TABLE_SLICES
    .map(([f, pos, size, rep]) =>
      `url(${uiAsset("table-long/" + f)}) ${pos} / ${size} ${rep}`)
    .join(",")
}
```

```html
<view class="long-table" style="{{longTableBg}}"></view>
```

```css
.long-table {
  border: 0;
  border-radius: 51rpx;            /* 必须等于角块的圆弧半径，见上表。原来写的 32rpx 是错的 */
  box-shadow: 0 14rpx 25rpx rgba(20, 14, 10, 0.45);   /* 同 3.1，必须保留 */
}
```

**边条已经镜像拼贴过**，`repeat-x` / `repeat-y` 不会有接缝——
生成图的边缘不可能天然无缝，切片时取一段边条水平镜像后接上，
得到的两倍宽图块按构造必然无缝。**不要自己重新裁切边条**，会破坏这个性质。

**已验证**：上面这套参数在 6 种真实比例（2.43 / 1.88 / 1.48 / 1.26 / 1.17 / 1.00）下
逐一渲染过，角块完整、边条无接缝、中心不变形。渲染脚本忠实模拟了 CSS 的层序、
双向平铺和 `border-radius` 裁剪，产物见 `generated/界面/_选片/长桌各形状.png`。

**已知的小瑕疵**：中心是 `cover` 裁切源图，而源图本身有轻微的光照梯度，
所以某些比例下中心偏右会有一道很淡的明暗台阶（1.88 和 1.26 那两档最明显）。
不影响使用，介意的话换一张光照更均匀的长桌重切即可。

**`border-image` 能把这 9 行压成 1 行**，切片还是同一套，而且它天然处理内缩、
不会有平铺铺到角块底下的问题。但**没在小程序 wxss 里验证过支不支持**——
先按多重 background 做，有空试一下，支持就换过去。

### 3.3 桌心纹章保持独立

`.table-emblem` 继续是绝对居中的独立节点，固定 `116rpx`，**不要烤进桌面图**——
长桌的比例是变的，烤进去的纹章会跟着 `cover` 浮动，极端比例还会被裁掉。

---

## 4. 底纹与配色

| 位置 | 现在 | 换成 |
|---|---|---|
| `.compact-table-stage`（`play.wxss:1249`） | `background: #d8d4ca` 浅灰 | `floor-<色>.jpg`，`center / cover`，内联 `background-image`。见 §4.1 |
| 首页 `page` / `.page` | 纯色 | `home-bg.jpg`，`center / cover`，内联 `background-image`。浅橡木 |

### 4.1 地毯四色可选

`floor-green` / `floor-indigo` / `floor-wine` / `floor-umber`，同规格只有颜色不同。

**建议做成个人偏好（本地 storage），不要做成房间级设置。** 理由：

- 它是**纯装饰**，不承载任何游戏信息，玩家之间看到不同颜色不影响对局
- 房间级要改房间数据结构和云函数（`missionSkin` / `roleSkin` 是那么做的，
  因为卡面必须全场一致）；个人偏好只要本地存一个 key，零服务端改动
- 玩家能随时换着玩，不用重开房

取图：`assets.uiAsset("floor-" + (floorColor || "indigo") + ".jpg")`。
默认值建议 `indigo`——四色里桌子和地面的明度差、冷暖差都最大，桌子最跳。

**连带必须改的一条：** 地面从浅灰换成深色绒毯后，
`.game-seat-token .seat-caption`（`play.wxss:1602`）的 `color: #343a36` 深色文字会消失在上面，
**要翻成浅色**（`#e6e2d8` 一档）。

### 结算页 `.verdict-mark`（result.wxml:4）

「光」/「影」两个字换成 `crest-good.png` / `crest-evil.png`，尺寸 `144rpx` 不变。

同时 `.good-verdict` / `.evil-verdict`（`result.wxss:6-7`）的绿/红渐变
**要改成中性深色渐变**——理由同 §2 第 2 条，阵营色现在由队徽承载。

---

## 5. 状态印 `seal-base.png`

9 个位置共用一张底：`.submitted-seal`（候/已/记/审/寂/杀，6 处）、
`.sigil-placeholder`（杯/封，2 处）、`.ceremony-mark`（夜，1 处）。

**汉字保留，叠在图上面。** 这些是抽象状态（等待中/已提交/已封存/沉寂/猎杀/首夜），
汉字对中文玩家零学习成本，图标化反而要学；而且每个印下面本来就紧跟一行标题文字。
换掉的只是原来那个「细金圈 + 粗体字」的 CSS 观感。

```css
.submitted-seal, .sigil-placeholder, .ceremony-mark {
  border: 0;                       /* 删掉原来的 3rpx 金圈 */
  background-position: center;
  background-size: contain;
  background-repeat: no-repeat;
  /* background-image 走云存储，在 js 里算好后内联，或改成一个铺满的 <image> 垫底 */
}
```

`.sigil-placeholder.sealed` / `.submitted-seal.spectator` 的灰化态，
可以继续用 `filter: grayscale(1)` 作用在同一张底上。

---

## 6. 不用动的

- **任务牌** `play.wxml:133` 已经接好了（`missionCardBack` 等），无需改动。
- **`.table-type-icon`**（首页圆桌/长桌选择器）是 CSS 画的一个圆和一个圆角矩形——
  它画的就是它本身，出图纯属倒退，保持现状。
- **`.role-card-glyph`**（房间页的小卡片图标）、**`.dial-key`**（数字键盘）同理。

---

## 7. 验收提示

改完之后，**按逻辑点 × DPR 去看，不要按逻辑点看**（`750rpx = 屏宽`，
所以 `68rpx` 在 375pt 宽的屏上是 34pt，真机渲染成 `34 × DPR` 个物理像素）。
按逻辑点缩图验收等于模拟一台不存在的非 Retina 手机，比真机严苛一倍。

几个关键尺寸：座位角标 17pt、任务指示器 34pt、桌心纹章 58pt、胜负印 72pt。
